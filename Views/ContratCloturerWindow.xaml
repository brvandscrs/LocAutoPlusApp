<Window x:Class="LocAutoPlusApp.Views.ContratCloturerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clôturer la location" Height="320" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F4F0E8">

    <Window.Resources>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height"          Value="38"/>
            <Setter Property="Padding"         Value="10,0"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="BorderBrush"     Value="#E8E8E4"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Background"      Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="BtnStyle" TargetType="Button">
            <Setter Property="Height"          Value="40"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="24">
        <StackPanel>
            <TextBlock Text="🏁  Clôturer la location"
                       FontSize="18" FontWeight="Bold"
                       FontFamily="Georgia" Margin="0,0,0,20"/>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Kilométrage retour *"
                               FontSize="11" Foreground="#999999"
                               FontFamily="Segoe UI" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtKmRetour"
                             Style="{StaticResource InputStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Date de retour *"
                               FontSize="11" Foreground="#999999"
                               FontFamily="Segoe UI" Margin="0,0,0,5"/>
                    <DatePicker x:Name="DpRetour"
                                Height="38" FontFamily="Segoe UI"
                                FontSize="13" BorderBrush="#E8E8E4"
                                Background="White"/>
                </StackPanel>
            </Grid>

            <TextBlock x:Name="TxtErreur"
                       Foreground="#C0392B" FontSize="12"
                       FontFamily="Segoe UI"
                       Visibility="Collapsed"
                       Margin="0,0,0,12"
                       TextWrapping="Wrap"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Annuler"
                        Style="{StaticResource BtnStyle}"
                        Background="#E8E8E4" Foreground="#555555"
                        Click="BtnAnnuler_Click"/>
                <Button Grid.Column="2" Content="🏁  Clôturer"
                        Style="{StaticResource BtnStyle}"
                        Background="#C9A84C" Foreground="#0F0F0F"
                        Click="BtnCloturer_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
