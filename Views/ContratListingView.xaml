<UserControl x:Class="LocAutoPlusApp.Views.ContratListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LocAutoPlusApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        
        <ListBox Grid.Column="0"  ItemsSource="{Binding Contrats}"
                     SelectedItem="{Binding SelectedContrat}"
                     DisplayMemberPath="FullName"
                     Height="400" />
        
        <StackPanel Grid.Column="1" Margin="20"
                    DataContext="{Binding SelectedContrat}">
            
            <TextBlock Text="{Binding Id}" />

            <TextBlock Text="{Binding UserId}" />

            <TextBlock Text="DateDebut" />
            <TextBox Text="{Binding DateDebut, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="DateFin" />
            <TextBox Text="{Binding DateFin, UpdateSourceTrigger=PropertyChanged}" />
            
            <TextBlock Text="Montant" />
            <TextBox Text="{Binding Montant, UpdateSourceTrigger=PropertyChanged}" />
            
            <TextBlock Text="EtatContrat" />
            <TextBox Text="{Binding EtatContrat, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</UserControl>
