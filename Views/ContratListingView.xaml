<UserControl x:Class="LocAutoPlusApp.Views.ContratListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LocAutoPlusApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        
        <ListBox Grid.Column="0"  ItemsSource="{Binding Contrats}"
                     SelectedItem="{Binding SelectedContrat}"
                     DisplayMemberPath="FullName"
                     Height="400" />
        
        <StackPanel Grid.Column="1" Margin="20"
                    DataContext="{Binding SelectedContrat}">
            <TextBlock Text="{Binding Id}" />
        </StackPanel>
    </Grid>
</UserControl>
