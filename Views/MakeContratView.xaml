<UserControl x:Class="LocAutoPlusApp.Views.MakeContratView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LocAutoPlusApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!--Ligne 1-->
        <TextBlock Grid.Row="0" Text="Ajouter un contrat" FontSize="24" />
        
        <!--Ligne 2-->
        <Grid Grid.Row="1" Margin="0 10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="UserId :" HorizontalAlignment="Left" />
            <TextBox Grid.Row="1" Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left" />
        </Grid>
        
        <!--Ligne 3-->
        <Grid Grid.Row="2" Margin="0 10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Montant :" HorizontalAlignment="Left" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Etat Contrat :" HorizontalAlignment="Left" />
            <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Montant, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" Margin="0 0 10 0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EtatContrat, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" />
        </Grid>
        
        <!--Ligne 4-->
        <Grid Grid.Row="3" Margin="0 10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Date Debut :" HorizontalAlignment="Left" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Date Fin :" HorizontalAlignment="Left" />
            <DatePicker Grid.Row="1" Grid.Column="0" SelectedDate="{Binding DateDebut, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" Margin="0 0 10 0"/>
            <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding DateFin, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" />
        </Grid>
        
        <!--Ligne 5-->
        <Grid Grid.Row="4" Margin="0 10 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Name="Enregistrer" Grid.Column="0" Command="{Binding EnregistrerCommand}" Content="Enregistrer" Width="100" HorizontalAlignment="Left" Margin="0 0 10 0"/>
            <Button Name="Annuler" Grid.Column="1" Command="{Binding AnnulerCommand}" Content="Annuler" Width="100" HorizontalAlignment="Left" />
        </Grid>
    </Grid>
</UserControl>
